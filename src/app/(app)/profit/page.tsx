export default function ProfitPage() {
  return (
    <div>
      <h1 style={{ fontSize: '1.25rem', fontWeight: 700, color: 'var(--p)', marginBottom: '1rem' }}>
        Profit associés
      </h1>
      <p style={{ color: 'var(--txtL)', fontSize: '.9rem' }}>
        Phase 4 — Contenu à migrer depuis public/index.html
      </p>
    </div>
  );
}
